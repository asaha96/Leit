# Local Postgres connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/leit

# JWT secret for local auth
JWT_SECRET=change-me

# Canvas API token (optional)
VITE_CANVAS_API_KEY=your_canvas_api_key

# Vite host/port handled by scripts; Supabase vars no longer used


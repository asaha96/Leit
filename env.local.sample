DATABASE_URL=postgres://localhost:5432/leit
JWT_SECRET=change-me
CANVAS_TOKEN_SECRET=change-me-too
# Optional: Canvas API token if you need Canvas proxy
# Optional: per-user Canvas token is set in Dashboard → Canvas tab
VITE_CANVAS_API_KEY=
# API origin for production (leave empty for dev; Vite proxy handles /api)
# Example: VITE_API_ORIGIN=https://api.yourdomain.com
VITE_API_ORIGIN=

# DeepSeek AI (optional - enables AI hints and explanations in Practice)
# Azure OpenAI endpoint format: https://your-resource.services.ai.azure.com/openai/v1/
DEEPSEEK_ENDPOINT=
DEEPSEEK_API_KEY=
DEEPSEEK_DEPLOYMENT=DeepSeek-V3.2

